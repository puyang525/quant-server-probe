# 社区数据贡献与隐私说明

[English](CONTRIBUTING_DATA.md) | [简体中文](CONTRIBUTING_DATA.zh-CN.md)

社区榜单的目标是用多个自愿公开的探针样本，比较不同国家、地区、云供应商和机房对量化交易基础设施的相对适用性。参与完全自愿，不参与不影响本项目的任何功能。

## 一眼看懂

- 默认不收集：运行 `probe`、`show` 或 `compare` 时，数据只保存在你的服务器上。
- 默认不上传：项目没有后台遥测，也不会静默发送报告。
- 明确选择加入：只有你另外执行 `public`、人工检查脱敏文件并主动提交 Pull Request，数据才会进入社区样本库。
- 不要提交完整报告：`probe-*.json`、`probe-*.md` 可能包含公网 IP、主机名、路由、对端地址和精确时间，只能留在本地。
- 可以撤回：贡献者可提交删除 Pull Request，或在仓库 Issue 中指明 `sample_id` 请求维护者删除。

## 如何自愿参加

先正常运行探针，再执行：

```bash
curl -fsSL https://raw.githubusercontent.com/puyang525/quant-server-probe/main/run.sh | \
  bash -s -- public /path/to/probe-report.json \
  --provider "供应商" --region "机房/云区域" --plan "实例规格" \
  --output public-sample.json
```

`public` 只生成本地脱敏文件，不会上传。请用文本编辑器检查内容，确认愿意公开其中的国家/地区、供应商、机房、套餐档位和测试统计后，再把文件放入 `public-samples/`，通过 GitHub Pull Request 提交。

如果不愿参与，到这里什么都不用做；也不需要添加任何“拒绝收集”参数，因为不上传就是默认行为。

## 自动移除和保留的内容

| 自动移除 | 为排名保留 |
|---|---|
| 源公网 IP、主机名 | 国家、地区代码 |
| CPU 精确型号 | 架构、vCPU 数、内存容量档位 |
| 路由跳点、DNS 和对端 IP | 用户填写的供应商、机房、套餐 |
| 原始错误文本 | TCP/HTTP/WS 延迟分位数与成功率 |
| 秒级精确时间 | 日期（精确到天） |
| 本地文件路径、账户或凭据 | 网络、性能和用途评分 |

探针本身不读取 API Key、钱包私钥或交易凭据；这些内容无论如何都不应写入或提交到仓库。

## 排名方法与限制

- 默认至少需要 3 个不同 `sample_id`，才展示一个“国家 + 供应商 + 机房”组合。
- 榜单采用中位数，不能用一次最低延迟制造排名。
- 样本数、测试日期、实例规格和测试版本必须同时显示；样本不足时只展示“数据不足”，不下结论。
- 同一贡献者、同一主机的反复测试可用于稳定性分析，但不应被伪装成多个独立用户。
- 后续应增加 p25/p75、失败率、版本分层和异常样本检测；社区榜单是筛选依据，不是性能承诺。
- 网络可达不等于账户可交易。排名不证明 IBKR、Binance、OKX、Polymarket 或其他平台对某个用户、账户主体、KYC 地区开放相关产品。

## 提交者确认

提交公开样本即表示你确认：

1. 你有权公开该脱敏测量数据；
2. 你已检查文件，确认不含凭据或不希望公开的信息；
3. 供应商、机房和套餐标签据你所知真实准确；
4. 数据不是为了刷榜而伪造或选择性篡改；
5. 你理解仓库公开后，第三方可能复制已经发布的数据，删除原仓库文件无法保证第三方副本同时消失。

维护者可拒绝格式错误、明显异常、重复、疑似伪造或可能泄露隐私的样本。
